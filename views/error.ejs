<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>404 Error: Time Capsule Mishap!</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #f8f9fa;
            background-image: radial-gradient(circle, #eef2f3, #c7d2db); /* Radial gradient background */
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            text-align: center;
        }
        .container {
            background-color: rgba(255, 255, 255, 0.9); /* Semi-transparent white background */
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
            max-width: 80%;
            text-align: center;
            animation: fadeIn 0.5s ease-out;
        }
        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }
        h1 {
            font-size: 3rem;
            color: #ff6b6b; /* Red */
            margin-bottom: 20px;
        }
        .wooden-box {
            position: relative;
            width: 300px; /* Increased size */
            height: 300px; /* Increased size */
            background-image: url('time-capsule.jpg'); /* Replace with your image URL */
            background-size: cover;
            background-position: center;
            border: 8px solid #5d4037; /* Darker brown */
            border-radius: 20px;
            overflow: hidden;
            cursor: pointer;
            display: flex;
            justify-content: center;
            align-items: center;
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.2);
            transition: transform 0.3s ease;
        }
        .wooden-box:hover {
            transform: scale(1.1);
        }
        .letter {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(255, 255, 255, 0.95); /* Semi-transparent white */
            color: #333;
            padding: 20px;
            box-sizing: border-box;
            font-size: 1rem;
            text-align: left;
            transform: translateY(-100%);
            transition: transform 0.6s ease;
            overflow-y: auto;
            z-index: -1;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
        }
        .wooden-box.opened .letter {
            transform: translateY(0);
            z-index: 1;
        }
        .error-message {
            font-size: 2rem;
            color: #ff6b6b; /* Red */
            text-align: center;
            margin-bottom: 10px;
        }
        .error-icon {
            font-size: 4rem;
            color: #ff6b6b; /* Red */
            margin-bottom: 20px;
        }
        .return-link {
            margin-top: 20px;
            text-align: center;
        }
        .return-link a {
            padding: 10px 20px;
            background-color: #007bff;
            color: #fff;
            text-decoration: none;
            border-radius: 5px;
            transition: background-color 0.3s ease;
        }
        .return-link a:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="wooden-box" onclick="revealLetter()">
            <div class="letter">
                <div class="error-icon">🕳️</div>
                <div class="error-message">404 Error: Time Capsule Mishap!</div>
                <p>Dear Explorer,</p>
                <p>It seems we've encountered a temporal disturbance, and this page has been misplaced in the time capsule.</p>
                <p>Please click the link below to return to the present timeline.</p>
                <p>Sincerely,<br>Your Time Capsule Team</p>
                <div class="return-link"><a href="/">Return to Present Timeline</a></div>
            </div>
        </div>
    </div>

    <script>
        function revealLetter() {
            var box = document.querySelector('.wooden-box');
            box.classList.toggle('opened');
        }
    </script>
</body>
</html>
