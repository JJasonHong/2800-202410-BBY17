<style>
    body {
            background-color: #c9d6ff;
            background: linear-gradient(to right, #e2e2e2, #c9d6ff);
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
            margin: 0;
        }

        .container {
            background-color: #fff;
            border-radius: 30px;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.35);
            overflow: hidden;
            width: 90%;
            max-width: 500px;
            min-height: 320px;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }

        .container p {
            color: #6683e0;
            font-size: 18px;
            margin-bottom: 20px;
        }

        .container form {
            width: 100%;
            max-width: 300px; /* Adjusted width */
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        .container form label {
            color: #333;
            font-size: 18px;
            margin-bottom: 10px;
        }

        .container form input {
            width: 100%;
            padding: 8px;
            border-radius: 5px;
            border: 1px solid #ccc;
            margin-bottom: 10px;
        }

        .container form button {
            background-color: #512da8;
            color: #fff;
            font-size: 16px;
            padding: 10px 45px;
            border: 1px solid transparent;
            border-radius: 8px;
            font-weight: 600;
            letter-spacing: 0.5px;
            text-transform: uppercase;
            cursor: pointer;
        }
</style>
<div class="container">
<h1>Reset Password</h1>
<p><i>Enter new password.</i></p>
    <form action="/resetPassword/<%= OTP %>" method="POST">
        <label for="newPassword">New Password:</label>
        <input type="password" id="newPassword" name="newPassword" required>
        <button type="submit">SAVE</button>
      </form>
</div>
      